import { conditionStyle } from 'map/helpers/condition-style';
import { module, test } from 'qunit';

module('Unit | Helper | condition style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = conditionStyle([42]);
  assert.ok(result);
});
